#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
REPO_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/../../../../.." && pwd)

exec "$REPO_ROOT/.venv/bin/bactrack" serve \
    --host "${BACSTOP_HOST:-0.0.0.0}" \
    --port "${BACSTOP_PORT:-8000}"
